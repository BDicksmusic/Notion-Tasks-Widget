# Cloudflare Worker Configuration
# Deploy with: npx wrangler deploy

name = "notion-tasks-webhook-relay"
main = "src/index.ts"
compatibility_date = "2024-01-01"

# KV Namespace for storing webhook events
[[kv_namespaces]]
binding = "WEBHOOK_EVENTS"
id = "a3d71577eabb4e2da7f607bb98880e75"

# Environment variables (set via wrangler secret)
# WEBHOOK_SECRET - Optional secret to validate requests from your app

